[pytest]
addopts = --alluredir=allure-results
testpaths = tests
markers =
    ui: UI layer tests (Playwright + POM)
    api: API layer tests (requests)
    e2e: End-to-end cross-layer tests
    db: Database layer tests (SQLite)
    high: High priority
    medium: Medium priority
    low: Low priority
